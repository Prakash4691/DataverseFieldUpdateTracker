[project]
name = "dataverse-field-update-tracker"
version = "0.1.0"
description = "Track field updates in Microsoft Dataverse"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
